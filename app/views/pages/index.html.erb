<!-- index.html.erb -->

<%= render :layout => 'pages/map', locals: {right_block: 'pages/events'} do %> 
  <%= gmaps({
    "markers" => {"data" => @markers.to_json},
    "map_options" => {
      :container_class => "stretch",
      :class => "stretch",
      :raw => '{ scrollwheel: false, mapTypeControl: false }'
    },
  }) %>
<% end %>